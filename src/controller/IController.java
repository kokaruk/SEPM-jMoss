package controller;

/**
 * @author dimz
 * @since 21/4/18.
 * contract interface for every controller to implement start method
 */
public interface IController {
    /**
     * entry point for every controller
     */
    void start();
}
